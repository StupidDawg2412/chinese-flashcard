{
  "name": "hsk-flashcards",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-data.js",
    "dev": "node scripts/generate-data.js && vite",
    "build": "node scripts/generate-data.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
